name: Build Solana Program

on:
  push:
    branches: [main]
    paths:
      - 'programs/**'
      - 'Anchor.toml'
      - '.github/workflows/build-program.yml'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: backpackapp/build:v2
    steps:
      - uses: actions/checkout@v4

      - name: Install Solana CLI
        run: |
          agave-install init 1.18.26 2>&1 || solana-install init 1.18.26 2>&1
          export PATH="/root/.local/share/solana/install/active_release/bin:$PATH"
          echo "/root/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH
          solana --version

      - name: Install Anchor CLI
        run: |
          cargo install --git https://github.com/coral-xyz/anchor avm --locked
          avm install 0.30.1
          avm use 0.30.1
          anchor --version

      - name: Build Program
        run: anchor build

      - name: List artifacts
        run: |
          ls -la target/deploy/
          ls -la target/idl/

      - name: Upload Program Binary
        uses: actions/upload-artifact@v4
        with:
          name: cumzillaraptors-program
          path: |
            target/deploy/cumzillaraptors.so
            target/deploy/cumzillaraptors-keypair.json
            target/idl/
          retention-days: 90
