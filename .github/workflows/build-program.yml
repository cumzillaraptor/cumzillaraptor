name: Build Solana Program

on:
  push:
    branches: [main]
    paths:
      - 'programs/**'
      - 'Anchor.toml'
      - '.github/workflows/build-program.yml'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Solana CLI
        run: |
          curl -sSfL https://release.anza.xyz/v1.18.26/install 2>&1 | sh -s -- v1.18.26
          export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH
          echo "PATH=$PATH" >> $GITHUB_ENV
          solana --version 2>&1 || solana-install --version 2>&1 || echo "Trying agave..."
          which solana || which solana-install || true

      - name: Install Anchor CLI
        run: |
          cargo install --git https://github.com/coral-xyz/anchor avm --locked
          avm install 0.30.1
          avm use 0.30.1
          anchor --version

      - name: Build Program
        run: anchor build

      - name: List artifacts
        run: |
          ls -la target/deploy/
          ls -la target/idl/

      - name: Upload Program Binary
        uses: actions/upload-artifact@v4
        with:
          name: cumzillaraptors-program
          path: |
            target/deploy/cumzillaraptors.so
            target/deploy/cumzillaraptors-keypair.json
            target/idl/
          retention-days: 90
